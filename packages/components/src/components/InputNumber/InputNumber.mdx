import {
  Meta,
  Story,
  Props,
  Status,
} from '../../../../../.storybook/components';

import * as Stories from './InputNumber.stories';

<Meta of={Stories} />

# InputNumber

<Status variant="experimental" />

A number field allows a user to enter a number, and increment or decrement the value using stepper buttons.

```tsx
import { InputNumber } from '@koobiq/react-components';
```

<Story of={Stories.Base} />

## Props

<Props of={Stories.Base} />

## Addons

You can add an extra content using the `startAddon` and `endAddon` props.

<Story of={Stories.Addons} />

## Variant

To change the visual state of the input, use the `variant` prop.

<Story of={Stories.Variant} />

## Invalid

The `isInvalid` prop toggles the error state. The `errorMessage` shows a message to explain the error to the user.

<Story of={Stories.Invalid} />

## Disabled

An input can be disabled by passing the `isDisabled` prop.

<Story of={Stories.Disabled} />

## Required

To make a field required, add the `isRequired` prop.
If the field has a label, a required indicator will appear next to it.

<Story of={Stories.Required} />

## Read only

The `isReadOnly` boolean prop makes the InputNumber's text content immutable.
Unlike `isDisabled`, the InputNumber remains focusable and the contents can still be copied.
See the [MDN docs](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/readonly) for more information.

<Story of={Stories.ReadOnly} />

## Full width

The `fullWidth` prop will make an input fit to its parent width.

<Story of={Stories.FullWidth} />

## Label placement and alignment

Use `labelPlacement` and `labelAlign` props to set the position of the label.

See the [Form](?path=/docs/components-form--docs#visual-options) for a two-column layout with labels on the side and a shared label column.

<Story of={Stories.LabelPlacementAlignment} />

## Value

### Default value

An InputNumber's value is empty by default, but an initial, uncontrolled, value can be provided using the `defaultValue` prop.

<Story of={Stories.DefaultValue} />

### Controlled value

The `value` prop can be used to make the value controlled.
The `onChange` event is fired when the user edits the text, and receives the new value.

<Story of={Stories.ControlledValue} />

## Number formatting

The default formatting style for `InputNumber` is decimal,
but you can configure various aspects via the `formatOptions` prop.
All options supported by [Intl.NumberFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/NumberFormat/NumberFormat) are supported, including configuration
of minimum and maximum fraction digits, sign display, grouping separators, etc.
Currently only standard notation is supported, though scientific, engineering, and compact
notation may be supported in the future.

<Story of={Stories.NumberFormatting} />

## Validation

InputNumber supports HTML validation attributes, custom validation logic, server errors,
and can be integrated with external form libraries. See the [Forms](?path=/docs/forms--docs) guide for details.

<Story of={Stories.Validation} />

### Minimum and maximum values

Use `minValue` and `maxValue` to set a range for InputNumber.
The value is clamped on blur, and the step buttons disable when the limit is reached.

<Story of={Stories.MinimumMaximumValues} />

### Step values

The `step` prop defines the increments for `NumberField`. Values snap to the nearest step on blur or when using the buttons,
starting from `minValue` (or 0 if none is set).

<Story of={Stories.StepValues} />
