import {
  Meta,
  Story,
  Props,
  Status,
} from '../../../../../.storybook/components';

import * as Stories from './Button.stories';

<Meta of={Stories} />

# Button

<Status variant="experimental" />

The Button is a clickable UI component that triggers actions or events.

```tsx
import { Button } from '@koobiq/react-components';
```

<Story of={Stories.Base} />

## Props

<Props of={Stories.Base} />

## Events

Button supports user interactions via mouse, keyboard, and touch.
You can handle all of these via the `onPress` prop. This is similar to the standard `onClick` event,
but normalized to support all interaction methods equally.

In addition, the `onPressStart`, `onPressEnd`, and `onPressChange` events are fired as the user interacts with the button.

## Variant

To change the visual state of the button, use the `variant` prop.

<Story of={Stories.Variant} />

## With icons

You can add an icon using the `startIcon` and `endIcon` props.

Both props expect an icon component from our [icon library](?path=/docs/icons--docs).

<Story of={Stories.WithIcons} />

## Only icon

A button can only consist of an icon.

<Story of={Stories.OnlyIcon} />

## Loading

Add a progress indicator to a button by using the `isLoading` prop.

<Story of={Stories.Loading} />

## Disabled

A button can be disabled by passing the `isDisabled` prop.

<Story of={Stories.Disabled} />

## Long label

If the button is too wide for its container, it shrinks, the text gets cut off, and an ellipsis is added at the end.

<Story of={Stories.LongLabel} />

## Full width

The `fullWidth` prop will make a button fit to its parent width.

<Story of={Stories.FullWidth} />

## Root tag

Use the `as` prop to redefine the HTML-tag of the component to your own.

<Story of={Stories.RootTag} />

## Customization

See an example of how to customize the button.

If needed, you can use your own **class names** and the component's helper
**data attributes** to define a custom button appearance.

<Story of={Stories.Custom} />

## Integration

### Next.js

Integrating navigation into a Button using Next.js Link:

```tsx
import NextLink from 'next/link';

type ButtonLinkProps = Omit<ButtonProps<'a'>, 'href'> & { href: string };

const ButtonLink = forwardRef<HTMLAnchorElement, ButtonLinkProps>(
  ({ href, ...other }, ref) => {
    return (
      <Button
        {...other}
        as={(props: typeof other) => (
          <NextLink {...props} href={href} ref={ref} />
        )}
      />
    );
  }
);

ButtonLink.displayName = 'ButtonLink';
```
