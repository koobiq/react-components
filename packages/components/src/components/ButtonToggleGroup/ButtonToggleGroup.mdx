import {
  Meta,
  Story,
  Props,
  Status,
} from '../../../../../.storybook/components';

import * as Stories from './ButtonToggleGroup.stories';

<Meta of={Stories} />

# ButtonToggleGroup

<Status variant="experimental" />

Display multiple options and allow users to select a single option.

```tsx
import { ButtonToggleGroup, ButtonToggle } from '@koobiq/react-components';
```

<Story of={Stories.Base} />

## Props

<Props />

## Icon

You can add an icon using the `icon` prop.

Both props expect an icon component from our [icon library](?path=/docs/icons--docs).

<Story of={Stories.Icon} />

## FullWidth

The `fullWidth` prop will make a component fit to its parent width.

<Story of={Stories.FullWidth} />

## Disabled

### Group

All buttons within a ButtonToggleGroup can be disabled using the `disabled` prop.

<Story of={Stories.DisabledGroup} />

### Item

Individual items can be disabled using the `disabled` prop on each ButtonToggle.

<Story of={Stories.DisabledItem} />

## LongText

<Story of={Stories.LongText} />

## EqualItemSize

<Story of={Stories.EqualItemSize} />

## Controlled selection

<Story of={Stories.ControlledSelection} />
