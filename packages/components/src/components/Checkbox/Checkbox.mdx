import {
  Meta,
  Story,
  Props,
  Status,
} from '../../../../../.storybook/components';

import * as Stories from './Checkbox.stories';

<Meta of={Stories} />

# Checkbox

<Status variant="experimental" />

A checkbox is used in forms when a user needs to select multiple values from a range of available options.

```tsx
import { Checkbox } from '@koobiq/react-components';
```

<Story of={Stories.Base} />

## Props

<Props of={Stories.Base} />

## Size

For `normal` or `big` checkboxes, use the `size` prop.

<Story of={Stories.Size} />

## Disabled

A checkbox can be disabled by passing the `isDisabled` prop.

<Story of={Stories.Disabled} />

## Value

### Default value

Checkboxes are not checked by default. The `defaultSelected` prop can be used to set the default state.

<Story of={Stories.DefaultValue} />

### Controlled value

The `isSelected` prop can be used to make the checked state controlled.
The `onChange` event is fired when the user presses the checkbox, and receives the new value.

<Story of={Stories.ControlledValue} />

## Invalid

The `isInvalid` prop toggles the invalid state.

<Story of={Stories.Invalid} />

## Read only

The `isReadOnly` prop makes the selection immutable. Unlike `isDisabled`, the Checkbox remains focusable.

<Story of={Stories.ReadOnly} />

## Indeterminate

A checkbox input can only have two states in a form: checked or unchecked.

Visually, there are three states a checkbox can be in: checked, unchecked, or indeterminate.

The `isIndeterminate` prop toggles the indeterminate state.

<Story of={Stories.Indeterminate} />

## Description

Add content to the children of the Checkbox component to add a description.

<Story of={Stories.Description} />

## Without label

You can use a component without a label, see the example.

<Story of={Stories.WithoutLabel} />

## HTML forms

Checkbox supports the name and value props for integration with HTML forms.

<Story of={Stories.HtmlForms} />
