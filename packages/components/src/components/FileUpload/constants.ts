/** Number of trailing characters kept intact when center-truncating a file name. */
export const FILE_NAME_TAIL_LENGTH = 7;

/**
 * Delay (ms) before returning focus to the input after the list empties. A
 * macrotask lets the empty-state (with its input) re-render first.
 */
export const FOCUS_RETURN_DELAY = 0;
