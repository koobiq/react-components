import {
  Props,
  Story,
  Meta,
  Status,
} from '../../../../../.storybook/components';

import * as Stories from './Tag.stories';

<Meta of={Stories} />

# Tag

<Status variant="experimental" />

Used for marking and categorization.

<Story of={Stories.Base} />

## Props

<Props />

## Variant

To change the visual state of the Tag, use the `variant` prop.

<Story of={Stories.Variant} />

## Icon

You can add an icon using the `icon` prop.

Prop expects an icon component from our [icon library](?path=/docs/icons--docs).

<Story of={Stories.Icon} />

## Disabled

Tag ca be disabled using the `isDisabled` prop.

<Story of={Stories.Disabled} />

## Close button

If you provide an `onClose` callback to the Tag, the component will display a close icon (✕).

<Story of={Stories.Cancel} />

## Long label

The text in the tag is always placed in one line.

If the tag is too wide for its container, it shrinks: the label text is cut off and ellipsis is added at the end.

<Story of={Stories.LongText} />
